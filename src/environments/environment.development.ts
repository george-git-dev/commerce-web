/**
 * Ambiente local. O backend hoje só roda na sua máquina (`http://localhost:8080`),
 * então este é o arquivo usado em `npm start` / `ng serve`.
 */
export const environment = {
  production: false,
  apiUrl: 'http://localhost:8080',
};