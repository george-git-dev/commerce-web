/**
 * Ambiente padrão (produção). Substituído por `environment.development.ts`
 * na configuração `development` do angular.json (fileReplacements).
 */
export const environment = {
  production: true,
  // TODO: atualizar quando o backend for publicado em produção.
  apiUrl: 'https://api.commerce-web.com.br',
};